# Format: [package] [platforms] [architectures]

git [platform:rpm]
jq [platform:rpm]
curl [platform:rpm]

libffi-devel [platform:rpm arch:x86_64]
libffi-dev [platform:dpkg arch:arm64]

# Fallback if primary package is unavailable
gcc || gcc-10 [platform:rpm]

